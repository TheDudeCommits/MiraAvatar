# Non-secret production process defaults.
# Inject credentials through the deployment environment; never commit them here.
# Required secrets: DATABASE_URL, OPENAI_API_KEY, ELEVENLABS_API_KEY,
# and a cryptographically random SESSION_SECRET. Set PUBLIC_APP_URL to the
# canonical HTTPS origin for wallet authentication.
NODE_ENV=production
PORT=5000
